export class PeriodDiagramData {
    period: Array<number>
    shots: Array<number>

    constructor(period: Array<number>, shots: Array<number>) {
        this.period = period
        this.shots = shots
    }
}